funcref write(fd: u8, str: u8, len: u64): none;

# this is a comment

test: u8 = 1;

funcdef start(argc: u8, argv: u8): u8 {
    char: u8 = 'AB';
    stdout: u8 = 1;
    world: u8[] = "world"; # this is an inline comment

    write(stdout, "Hello, ", 7);
    write(stdout, world, 5);
    write(stdout, "\n", 1);

    return (0);
}